name: tests

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  unittest:
    name: ${{ matrix.module }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        module:
          - bad_pass
          - daily_files
          - finalizer
          - oer
          - pipeline_init
          - unifier
          - xover
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: false

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: |
            pipeline/pipeline_runtime/requirements.txt
            pipeline/daily_file_gen/${{ matrix.module }}/requirements.txt
            setup.py

      - name: Install pipeline_runtime base
        run: pip install -r pipeline/pipeline_runtime/requirements.txt

      - name: Install ${{ matrix.module }} requirements
        run: pip install -r pipeline/daily_file_gen/${{ matrix.module }}/requirements.txt

      - name: Install shared utilities (editable)
        run: pip install -e .

      - name: Run unittests
        working-directory: pipeline/daily_file_gen/${{ matrix.module }}
        run: python -m unittest discover -s tests -t . -v
        
  failure_handling:
    name: failure_handling
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: false

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install boto3
        run: pip install boto3

      - name: Run unittests
        working-directory: pipeline/infra/failure_handling
        run: python -m unittest discover -s tests -t . -v

  run_summary:
    name: run_summary
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: false

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: setup.py

      # run_summary ships utilities; the editable install pulls boto3 + pyyaml.
      - name: Install shared utilities (editable)
        run: pip install -e .

      - name: Run unittests
        working-directory: pipeline/infra/run_summary
        run: python -m unittest discover -s tests -t . -v

  # other_products module. enso is deliberately not wired yet: its existing
  # test_enso.py depends on LFS data (data/simple_grids/...) and the checkout
  # uses lfs: false, so a discover job there would go red until that test is
  # made CI-safe.
  simple_grids:
    name: simple_grids
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: false

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: |
            pipeline/pipeline_runtime/requirements.txt
            pipeline/other_products/simple_grids/requirements.txt
            setup.py

      - name: Install pipeline_runtime base
        run: pip install -r pipeline/pipeline_runtime/requirements.txt

      - name: Install simple_grids requirements
        run: pip install -r pipeline/other_products/simple_grids/requirements.txt

      - name: Install shared utilities (editable)
        run: pip install -e .

      - name: Run unittests
        working-directory: pipeline/other_products/simple_grids
        run: python -m unittest discover -s tests -t . -v

  utilities:
    name: utilities
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: false

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: setup.py

      - name: Install shared utilities (editable)
        run: pip install -e .

      # Runs the Target registry tests, including the consistency check that
      # pins targets.yaml to the filesystem (the registry's drift guard).
      - name: Run unittests
        run: python -m unittest discover -s utilities/tests -t . -v

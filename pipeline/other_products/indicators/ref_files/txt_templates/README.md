# Indicators txt templates

This directory contains four template txt files, each containing the HDR information for the given indicator. 

When template file is used to generate a new indicator.txt file, the `PLACEHOLDER_CREATION_DATE` string gets replaced by the `YYYY-MM-DD` date at runtime.
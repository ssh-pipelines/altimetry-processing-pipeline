# pipeline_runtime — shared scientific stack
#
# Pinned versions of libraries needed by every stage that reads or writes a
# daily file (NetCDF). The single contract that keeps producer and consumer
# stages aligned on encoding behavior.
#
# Consumers: bad_pass, xover, daily_files, oer, finalizer, simple_grids,
#            indicators, enso
# Excluded:  pipeline_init, unifier (no NetCDF I/O; stay lean for cold start)
#
# Per-stage extras (pandas, dask, geopandas, pyresample, cartopy, ...) live in
# each stage's own requirements.txt and install on top of this base.

numpy==2.2.6
xarray==2025.4.0
netCDF4==1.7.2
h5netcdf==1.6.1
h5py==3.13.0
scipy==1.15.3
boto3==1.28.84
s3fs
pyyaml
